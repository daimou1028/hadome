---
type: review
target_sha: 37dfc96
base_sha: 8f2769d
verdict: warn
reviewed_at: 2026-09-12_1200
files_changed: 3
---

# Review: PR #2 — refactor: 拆解 handleRun() 巨型函式（T-005）

`feature/拆解handleRun` → `develop`

## Phase 2：靜態檢查

| 檢查 | 結果 |
|------|------|
| lint | 本專案未設定 lint（既有狀態，非本 PR 引入的缺口）|
| test | 本專案在此分支上還沒有測試框架（T-007 在平行的 PR #4，尚未合併）；改用 `node --check extension.js` 重新驗證語法，通過 |

## Phase 3：Diff review

| 檔案 | 變更 | 備註 |
|------|------|------|
| `extension.js` | +490/-401（`git diff --stat`）| 見下方「單檔 >500 行」說明 |
| `report/T-005_..._執行報告.md` | 新增 | task-report，格式正確 |
| `todo/todo.md` | +1/-1 | T-005 標記完成 |

檢查項：

- [x] 無 `console.log`/`print` 殘留
- [x] 無未處理 TODO/FIXME
- [x] 無 SQL 變更
- ⚠️ **單檔變更 818 行（`git diff --stat` 原始行數），超過 500 行門檻** — 但用 `git diff --ignore-all-space` 重新量測，扣除縮排後只有 24 行是真正字元層級的變更（都集中在 4 個新函式的簽名/`return`/收尾/呼叫點），其餘 794 行全部是「內容搬進巢狀函式後多縮排 2 格」造成的純空白差異。已獨立重跑 `--ignore-all-space` 確認，數字與 PR 描述一致。**這是縮排造成的視覺放大，不是真的難審查的變更量**，但技術上仍觸發規則門檻，如實記錄。
- [x] 無測試檔案被刪除
- [x] 無 `.env`/secrets 變更
- [x] 無新依賴

## 功能驗證（獨立重跑，非沿用實作時的結果）

- `node --check extension.js` → 通過
- 抽查 `setupMcp`/`buildTaskAttachments`/`buildAgentCallbacks`/`finishRun` 四個邊界，確認閉包變數（`mcpGot`、`taskText`/`uploadFiles`、`sendingInstruction`、`result`）作用域正確

## 已知限制

本專案目前沒有 VS Code extension host 測試環境，無法對 `handleRun()` 的實際執行路徑做整合測試。這個限制在規格書與執行報告中已載明，不是本次審查新發現的問題。

## Verdict: `warn`

無 `fail` 級問題。`warn` 純粹來自「單檔案 diff 行數」這條機械式規則，實質風險已用 `--ignore-all-space` 交叉驗證排除。**可以合併**，`warn` 只是提醒之後看 diff 的人善用 `--ignore-all-space` 而非逐行肉眼比對 818 行。
